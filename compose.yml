services:
  feedhook:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - DATABASE_URL=sqlite:///data/feedhook.db
      - PORT=8080
    restart: unless-stopped
